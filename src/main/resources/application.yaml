spring:
  application:
    name: demo-spring-liquibaseyaml-gradle

  datasource:
    url: jdbc:postgresql://localhost:5432/demo1
    username: postgres
    password: postgres
  liquibase:
    change-log: classpath:/db/changelog/root.yaml